{% block subject %}Thank you for contacting us{% endblock %}

{% block body_html %}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            color: #212529;
        }
        .intro {
            margin-bottom: 25px;
            font-size: 16px;
        }
        .summary {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .summary h2 {
            margin-top: 0;
            font-size: 18px;
            color: #495057;
        }
        .field {
            margin-bottom: 12px;
        }
        .field-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 3px;
        }
        .field-value {
            color: #212529;
        }
        .message-box {
            background-color: #ffffff;
            padding: 12px;
            border-radius: 4px;
            border-left: 3px solid #0d6efd;
            white-space: pre-wrap;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 14px;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>✅ Thank you for contacting us</h1>
    </div>

    <div class="intro">
        <p>Dear {% if data.name is defined %}{{ data.name }}{% else %}valued customer{% endif %},</p>
        <p>We have received your message and will get back to you as soon as possible. A member of our team will review your inquiry and respond shortly.</p>
    </div>

    <div class="summary">
        <h2>Summary of your message:</h2>

        {% if data.name is defined %}
        <div class="field">
            <div class="field-label">Name:</div>
            <div class="field-value">{{ data.name }}</div>
        </div>
        {% endif %}

        {% if data.email is defined %}
        <div class="field">
            <div class="field-label">Email:</div>
            <div class="field-value">{{ data.email }}</div>
        </div>
        {% endif %}

        {% if data.phone is defined %}
        <div class="field">
            <div class="field-label">Phone:</div>
            <div class="field-value">{{ data.phone }}</div>
        </div>
        {% endif %}

        {% if data.subject is defined %}
        <div class="field">
            <div class="field-label">Subject:</div>
            <div class="field-value">{{ data.subject }}</div>
        </div>
        {% endif %}

        {% if data.message is defined %}
        <div class="field">
            <div class="field-label">Message:</div>
            <div class="message-box">{{ data.message }}</div>
        </div>
        {% endif %}

        {% for key, value in data %}
            {% if key not in ['name', 'email', 'phone', 'subject', 'message'] %}
            <div class="field">
                <div class="field-label">{{ key|title }}:</div>
                <div class="field-value">{{ value }}</div>
            </div>
            {% endif %}
        {% endfor %}
    </div>

    <div class="footer">
        <p>This is an automated message. Please do not reply directly to this email.</p>
        <p>If you have any questions, please contact us through our website or the contact details provided.</p>
    </div>
</body>
</html>
{% endblock %}
