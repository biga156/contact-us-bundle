{#
    Email verification template (plain text)
    Variables from ContactMailer:
    - message: ContactMessage object
    - data: array of form data
    - subject: email subject
    - verificationUrl: full URL for verification
    - token: verification token
    - labels: pre-translated labels array
#}
{{ labels.verification_title }}
================================================================================

{{ labels.verification_intro }}

---
{{ labels.verification_your_message }}
---

{% if data.name is defined and data.name %}
{{ labels.label_name }}: {{ data.name }}
{% endif %}
{% if data.email is defined and data.email %}
{{ labels.label_email }}: {{ data.email }}
{% endif %}
{% if data.phone is defined and data.phone %}
{{ labels.label_phone }}: {{ data.phone }}
{% endif %}
{% if data.subject is defined and data.subject %}
{{ labels.label_subject }}: {{ data.subject }}
{% endif %}
{% if data.message is defined and data.message %}

{{ labels.label_message }}:
{{ data.message }}
{% endif %}

---
⚠️ {{ labels.verification_warning_title }}
---
{{ labels.verification_warning_text }}

---
{{ labels.verification_button }}
---
{{ verificationUrl }}

---
{{ labels.verification_footer }}
{{ labels.verification_not_you }}
